[project]
name = "layout-compiler"
version = "0.1.0"
description = "Phase 4: confirmed brief + Commit #0 existing conditions -> validated ChapterLayout phase=new (PLAN.md Part E Phase 4; LLM behind an interface, mocked in CI)"
requires-python = ">=3.12"
dependencies = [
    "chapter-contracts",
    "anthropic>=1,<2",
    "fastapi>=0.115,<1",
    "uvicorn>=0.35,<1",
    "shapely>=2.0,<3",
]

[dependency-groups]
dev = [
    "pytest>=8.4,<9",
    "hypothesis>=6.100,<7",
    "ruff>=0.13,<0.15",
    "httpx>=0.28,<1",
]

[tool.uv.sources]
chapter-contracts = { path = "../../packages/contracts/python", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/layout_compiler"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
