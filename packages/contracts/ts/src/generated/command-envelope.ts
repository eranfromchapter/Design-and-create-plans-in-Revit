/* eslint-disable */
// GENERATED from schemas/command-envelope.v1.json by scripts/codegen.mjs — DO NOT EDIT.
import { z } from "zod"

export const commandEnvelopeSchema = z.object({ "payload": z.string().min(2).max(2000000).describe("EnvelopeBody as a JSON string, produced once by the gateway"), "sig": z.string().regex(new RegExp("^[0-9a-f]{128}$")).describe("hex Ed25519 signature (RFC 8032) over the UTF-8 bytes of payload, made with the per-project private key held only by the gateway") }).strict().describe("Wire format for signed command envelopes. The gateway serializes EnvelopeBody ONCE (RFC 8785 canonical JSON) into `payload` and signs it with the per-project Ed25519 private key (held only by the gateway); verifiers (plugin, revit-sim) check the Ed25519 signature over the exact received UTF-8 bytes of `payload` BEFORE any parsing, then parse and validate the body against $defs/EnvelopeBody and each op's args against ops/registry.json. Executors hold only the per-project PUBLIC key, delivered at workstation enrollment over the authenticated WSS channel (auth_ok.signing_public_key) and integrity-pinned locally; rotation = new keypair + re-delivery with a dual-accept grace window (Phase 10 runbook). Cross-language canonicalization is never a verification dependency.")
export type CommandEnvelope = z.infer<typeof commandEnvelopeSchema>
